---
title: Playground
description: 설정을 직접 바꿔보기
---

import Playground from '../../components/Playground.astro';

컨트롤을 바꾸면 아래 코드가 화면에 적용된 실제 상태에서 다시 만들어진다 — 손으로 쓴 게
아니라서 어긋날 수 없다.

`loop`·`autoplay`·`effect`는 `init()` 시점에 배선되므로, 바꿀 때마다 `destroy()` 후
`Carousel.init()`으로 다시 만든다.

<Playground />
